---
phase: 02.1-history-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/App.vue, frontend/src/components/DebateSessionView.vue, frontend/src/components/EvidenceBoard.vue, frontend/src/style.css]
autonomous: true
requirements: [HIST-02, HIST-03, HIST-04, READ-03, COMP-01]
user_setup: []
must_haves:
  truths:
    - "Frontend has one shared center-pane renderer that can display either a saved session snapshot or a live streaming debate without duplicating the detail UI"
    - "If no id_card has been submitted and no history item is selected, the center pane stays empty instead of showing stale prior data"
    - "Saved-session evidence, round history, and final conclusion are rendered through the same presentation path already used for live debate output"
    - "Current /api/debate_stream behavior remains usable for the existing live analysis flow"
  artifacts:
    - path: "frontend/src/components/DebateSessionView.vue"
      provides: "Shared session detail renderer for evidence, round history, and final outcome"
      min_lines: 80
    - path: "frontend/src/App.vue"
      provides: "Restructured app-level state that cleanly separates live, saved, and empty center-pane states"
      min_lines: 220
  key_links:
    - from: "frontend/src/App.vue"
      to: ".planning/phases/02.1-history-ui/02.1-CONTEXT.md"
      via: "empty-state and shared-renderer decisions follow the locked Phase 2.1 context"
      pattern: "history|selected|loading"
    - from: "frontend/src/components/DebateSessionView.vue"
      to: "frontend/src/components/AgentCard.vue"
      via: "saved and live judgments reuse the same agent-card rendering path"
      pattern: "AgentCard"
---

<objective>
Refactor the current frontend so the center pane can render one normalized debate session view for both live runs and saved-session detail.

Purpose: Create a stable UI/state foundation before wiring the history sidebar and selection interactions.
Output: A shared center-pane renderer plus app-level state that can represent empty, live, and saved-session modes without regressing the current streaming path.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-history-ui/02.1-CONTEXT.md
@.planning/phases/02.1-history-ui/02.1-RESEARCH.md
@frontend/src/App.vue
@frontend/src/components/EvidenceBoard.vue
@frontend/src/components/AgentCard.vue
@frontend/src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a normalized center-pane session model</name>
  <files>frontend/src/App.vue, .planning/phases/02.1-history-ui/02.1-CONTEXT.md</files>
  <action>Replace the current one-off `currentData` handling with app-level state that can represent: no selection yet, a live run in progress, and one loaded saved session. Keep separate loading and error fields for live streaming versus saved-session reads so one path cannot accidentally erase the other.</action>
  <verify>Review the new state shape and transitions in `App.vue` to confirm empty-state semantics, live-run semantics, and saved-session semantics are explicit and not encoded through fragile implicit null checks</verify>
  <done>The app has a clean, explicit state model for empty, live, and saved center-pane rendering</done>
</task>

<task type="auto">
  <name>Task 2: Extract a shared session detail renderer for evidence and debate history</name>
  <files>frontend/src/components/DebateSessionView.vue, frontend/src/components/EvidenceBoard.vue, frontend/src/App.vue</files>
  <action>Introduce a dedicated center-pane component that renders evidence, round history, and the final decision for one normalized session payload. Reuse `EvidenceBoard` and `AgentCard` rather than forking a separate historical-detail presentation. Ensure the new component can accept both a just-streamed live result and a loaded saved snapshot.</action>
  <verify>Inspect the component props and template to confirm that saved-session detail and live debate output now share one rendering path and that evidence is no longer confined to the left pane</verify>
  <done>One reusable session-view component renders both live and saved debates consistently</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the current live submission experience while moving toward the new layout</name>
  <files>frontend/src/App.vue, frontend/src/style.css</files>
  <action>Keep the existing input-driven `/api/debate_stream` flow intact while updating the page layout and state wiring. The user should still be able to enter an `id_card`, watch evidence appear, see per-round agent updates, and view the final conclusion in the center pane after the refactor.</action>
  <verify>Run the frontend build and inspect the refactored live path to confirm streaming handlers still populate the shared session renderer instead of breaking due to the state extraction</verify>
  <done>The new shared renderer foundation is in place without regressing current live streaming behavior</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Center-pane rendering is shared between live sessions and saved-session detail
- [ ] Empty state remains blank when nothing has been submitted or selected
- [ ] Evidence is available in the shared center-pane view rather than locked to the sidebar
- [ ] Existing live `/api/debate_stream` behavior still renders correctly through the refactored state
- [ ] Frontend build passes
</verification>

<success_criteria>

- All tasks completed
- The UI has one reusable detail renderer before history-list wiring begins
- Empty/live/saved page states are explicit and stable
- The existing live debate experience remains intact after the refactor

</success_criteria>

<output>
After completion, create `.planning/phases/02.1-history-ui/02.1-01-SUMMARY.md`
</output>
