---
phase: 02.1-history-ui
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified: [frontend/src/App.vue, frontend/src/components/HistorySessionList.vue, frontend/src/style.css, frontend/package.json]
autonomous: true
requirements: [HIST-01, HIST-03, HIST-04, READ-03, COMP-01]
user_setup: []
must_haves:
  truths:
    - "Left pane shows a compact saved-session history list for the current id_card instead of the old evidence-only sidebar"
    - "Selecting one history row loads saved debate detail through the Phase 2 retrieval APIs without rerunning agents"
    - "Submitting an id_card still launches the live debate stream, and the history list refreshes after completion so the new session becomes visible immediately"
    - "History loading failures are localized and do not wipe the rest of the app shell"
  artifacts:
    - path: "frontend/src/components/HistorySessionList.vue"
      provides: "Compact sidebar list for saved-session summaries with active selection state"
      min_lines: 80
    - path: "frontend/src/App.vue"
      provides: "History fetch, detail fetch, auto-select, and post-stream refresh wiring over the shared renderer foundation"
      min_lines: 260
  key_links:
    - from: "frontend/src/App.vue"
      to: "api/main.py"
      via: "frontend consumes GET /api/debates and GET /api/debates/{session_id} alongside POST /api/debate_stream"
      pattern: "/api/debates|/api/debate_stream"
    - from: "frontend/src/components/HistorySessionList.vue"
      to: ".planning/phases/02.1-history-ui/02.1-CONTEXT.md"
      via: "sidebar layout and summary-row density follow the locked Phase 2.1 history-list decisions"
      pattern: "history list|summary"
---

<objective>
Wire the saved-session APIs into the refactored frontend so the left pane shows history, selections load detail into the center pane, and live runs refresh that history in place.

Purpose: Turn the new backend retrieval APIs into a usable in-app browsing experience without sacrificing the current live analysis flow.
Output: A history sidebar, saved-session detail loading, newest-session auto-selection behavior, and post-stream refresh for the current `id_card`.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-history-ui/02.1-CONTEXT.md
@.planning/phases/02.1-history-ui/02.1-RESEARCH.md
@.planning/phases/02.1-history-ui/02.1-01-PLAN.md
@frontend/src/App.vue
@api/main.py
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the saved-session history sidebar over the summary API</name>
  <files>frontend/src/components/HistorySessionList.vue, frontend/src/App.vue, frontend/src/style.css</files>
  <action>Create a compact left-side history list that consumes `GET /api/debates?id_card=...` summary rows. Show conclusion, stance, completion time, source endpoint, and compact round/consensus metadata. Make the selected row visually obvious without turning the sidebar into a second detail pane.</action>
  <verify>Review the sidebar rendering and data mapping to confirm it stays summary-only, uses the current id_card scope, and does not attempt to embed full evidence or round detail in the list itself</verify>
  <done>The left pane presents saved sessions as a compact, selectable history list</done>
</task>

<task type="auto">
  <name>Task 2: Wire saved-session selection and auto-selection behavior</name>
  <files>frontend/src/App.vue, frontend/src/components/HistorySessionList.vue</files>
  <action>On history-row click, fetch `GET /api/debates/{session_id}` and hydrate the shared center renderer from the saved snapshot. When an id_card with history is loaded and nothing is currently selected, auto-select the newest session in the first version. Keep detail-loading and history-loading states separate so the center pane is not wiped unnecessarily during sidebar refreshes.</action>
  <verify>Inspect the selection flow to confirm one click triggers detail fetch instead of a rerun, newest-first auto-selection works only when appropriate, and history fetch failures remain localized</verify>
  <done>Saved-session browsing behaves predictably through explicit selection and first-load auto-selection</done>
</task>

<task type="auto">
  <name>Task 3: Refresh history after live streaming completes and verify the end-to-end UI flow</name>
  <files>frontend/src/App.vue, frontend/package.json</files>
  <action>After a live `/api/debate_stream` run completes, refresh the current id_card history list so the new saved session appears immediately at the top. Verify the phase through at least the build path and the key UI journeys: blank initial state, history selection, live run, and post-run refresh. Only add new frontend test dependencies if they are strictly needed for a small, focused smoke check and do not derail the phase.</action>
  <verify>Run the frontend build and confirm the app supports the four locked user journeys without regressing the streaming path</verify>
  <done>The saved-session UI is fully wired and stays in sync with newly completed live runs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Left pane shows saved-session history summaries for the current `id_card`
- [ ] Clicking a history item loads saved detail through retrieval APIs instead of rerunning agents
- [ ] The newest session auto-selects on first history load when appropriate
- [ ] A completed live stream refreshes the sidebar and surfaces the new saved session
- [ ] Frontend build passes and the key UI flows are validated
</verification>

<success_criteria>

- All tasks completed
- Phase 2.1 delivers the in-app history browsing experience defined in context
- Saved-session detail is now reachable from the frontend without rerunning debates
- Live streaming and saved-session browsing coexist cleanly in one app shell

</success_criteria>

<output>
After completion, create `.planning/phases/02.1-history-ui/02.1-02-SUMMARY.md`
</output>
