---
phase: 02-retrieval-apis
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [api/main.py, tests/test_retrieval_api.py, agents/debate_persistence.py]
autonomous: true
requirements: [COMP-01, COMP-02, READ-03]
user_setup: []
must_haves:
  truths:
    - "Backend exposes exactly two new retrieval entry points in Phase 2: history by id_card and detail by session_id"
    - "New retrieval routes reuse the read helpers from the persistence layer instead of embedding ad hoc SQL in FastAPI handlers"
    - "List misses return 200 with an empty list, while detail misses return HTTP 404"
    - "Existing /api/debate and /api/debate_stream behaviors remain unchanged for current callers"
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI retrieval endpoints that preserve current API style"
      contains: "/api/debate"
    - path: "tests/test_retrieval_api.py"
      provides: "Route-level tests for list/detail retrieval behavior and compatibility"
      min_lines: 80
  key_links:
    - from: "api/main.py"
      to: "agents/debate_persistence.py"
      via: "new GET handlers delegate to shared retrieval helpers"
      pattern: "debate_persistence"
    - from: "tests/test_retrieval_api.py"
      to: "api/main.py"
      via: "route tests lock the public retrieval contract"
---

<objective>
Expose the Phase 2 retrieval APIs without breaking the current live debate experience.

Purpose: Make saved debate sessions consumable through stable backend endpoints while preserving compatibility with existing write/live endpoints.
Output: Two retrieval endpoints, one for history-by-`id_card` and one for detail-by-`session_id`, with route-level coverage for success, empty, and not-found behavior.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-retrieval-apis/02-CONTEXT.md
@.planning/phases/02-retrieval-apis/02-01-PLAN.md
@api/main.py
@agents/debate_persistence.py
@tests/test_debate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list and detail retrieval endpoints with stable route semantics</name>
  <files>api/main.py, agents/debate_persistence.py</files>
  <action>Add exactly two new read endpoints for Phase 2. Use GET semantics and keep them adjacent to the existing debate routes. The list endpoint should return a success envelope with a summary-only history array for one `id_card`. The detail endpoint should return one snapshot-first saved debate payload for one `session_id`, and convert the shared not-found signal into HTTP 404.</action>
  <verify>Review the route signatures and handler bodies to confirm there are only two new retrieval endpoints, the list path returns an empty array on no history, and the detail path raises 404 on a missing session</verify>
  <done>FastAPI exposes the two Phase 2 retrieval routes with the locked response semantics</done>
</task>

<task type="auto">
  <name>Task 2: Preserve existing debate entry points and response style</name>
  <files>api/main.py</files>
  <action>Keep `/api/debate` and `/api/debate_stream` behavior intact while adding the retrieval routes. Match the existing backend response envelope style for non-stream endpoints so current and future consumers do not have to juggle conflicting API conventions.</action>
  <verify>Compare the existing debate handlers before and after the change and confirm the new retrieval endpoints follow the same success-envelope style without altering current live route signatures</verify>
  <done>Retrieval is added without regressing current debate/live API behavior</done>
</task>

<task type="auto">
  <name>Task 3: Add route-level regression tests for retrieval behavior</name>
  <files>tests/test_retrieval_api.py, api/main.py</files>
  <action>Create route-level tests that cover: history success, history empty-list behavior, detail success, and detail 404 behavior. Stub the shared retrieval helpers rather than requiring a live database. Keep the tests focused on public API contracts and compatibility rather than persistence internals already covered by Plan 02-01.</action>
  <verify>Run the retrieval API tests and confirm the assertions point clearly at public contract regressions instead of internal implementation details</verify>
  <done>Route-level regression coverage exists for the new retrieval endpoints and their locked semantics</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Two retrieval endpoints exist and no convenience/filter endpoints leak into Phase 2
- [ ] History-by-id_card returns a success envelope with a summary-only list
- [ ] Detail-by-session_id returns a snapshot-first payload or HTTP 404 when missing
- [ ] Existing `/api/debate` and `/api/debate_stream` handlers remain compatible
- [ ] Retrieval API route tests pass
</verification>

<success_criteria>

- All tasks completed
- Phase 2 exposes exactly the two retrieval endpoints defined in context
- Public retrieval responses are consistent enough for future frontend history usage
- Existing live debate callers remain unaffected by the new read paths

</success_criteria>

<output>
After completion, create `.planning/phases/02-retrieval-apis/02-02-SUMMARY.md`
</output>
