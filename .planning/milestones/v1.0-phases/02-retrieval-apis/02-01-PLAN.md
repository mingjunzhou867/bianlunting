---
phase: 02-retrieval-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agents/debate_persistence.py, tests/test_persistence_contract.py, .planning/phases/02-retrieval-apis/02-CONTEXT.md]
autonomous: true
requirements: [READ-01, READ-02, READ-03]
user_setup: []
must_haves:
  truths:
    - "Backend has one reusable read layer for saved debate sessions before any FastAPI route wiring"
    - "History-by-id_card reads summary-only rows from debate_session and sorts newest first by completed_at"
    - "Detail-by-session_id uses snapshot_payload as the canonical saved detail source instead of reconstructing from logs by default"
    - "Missing id_card history returns an empty list, while missing session_id raises an explicit not-found signal for the API layer"
  artifacts:
    - path: "agents/debate_persistence.py"
      provides: "Saved-session list/detail query helpers and deserialization logic"
      min_lines: 80
    - path: "tests/test_persistence_contract.py"
      provides: "Focused retrieval contract checks for summary rows and snapshot-first detail loading"
      min_lines: 80
  key_links:
    - from: "agents/debate_persistence.py"
      to: "data/schema/mysql_ddl.sql"
      via: "read helpers match debate_session columns and snapshot_payload semantics"
      pattern: "debate_session|snapshot_payload"
    - from: "agents/debate_persistence.py"
      to: ".planning/phases/02-retrieval-apis/02-CONTEXT.md"
      via: "summary-only list and snapshot-first detail follow locked Phase 2 decisions"
---

<objective>
Build the reusable backend read/query layer for saved debate sessions before exposing any new API endpoints.

Purpose: Keep retrieval logic centralized and aligned with the Phase 1 persistence contract so the API layer can stay thin.
Output: Query helpers that list saved session summaries by `id_card`, load one saved session detail by `session_id`, and deserialize the saved snapshot payload consistently.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-retrieval-apis/02-CONTEXT.md
@agents/debate_persistence.py
@data/schema/mysql_ddl.sql
@config/database.py
@tests/test_persistence_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical retrieval contract over debate_session</name>
  <files>agents/debate_persistence.py, data/schema/mysql_ddl.sql, .planning/phases/02-retrieval-apis/02-CONTEXT.md</files>
  <action>Map the Phase 2 decisions into concrete read shapes. Lock the summary fields that the `id_card` history list returns and the detail object that the `session_id` lookup returns. Keep the detail contract snapshot-first, with `snapshot_payload` as the canonical source and `agent_debate_log` treated as supporting data rather than the default assembly path.</action>
  <verify>Review the resulting helper signatures and returned field names against the Phase 2 context decisions for summary-only history and snapshot-first detail</verify>
  <done>A stable retrieval contract exists for both history lists and one-session detail lookups</done>
</task>

<task type="auto">
  <name>Task 2: Implement saved-session query helpers and explicit not-found behavior</name>
  <files>agents/debate_persistence.py</files>
  <action>Add reusable query helpers that load summary rows from `debate_session` by `id_card` ordered by newest completion time, plus one detail loader by `session_id`. Deserialize `snapshot_payload` into a Python object for detail reads, and introduce an explicit not-found signal for missing sessions so the API layer can translate it into HTTP 404.</action>
  <verify>Inspect the SQL and helper code to confirm list queries hit `debate_session`, ordering is newest first, and missing detail lookups cannot quietly return malformed empty payloads</verify>
  <done>The backend has reusable list/detail read helpers with explicit missing-session behavior</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regression checks for retrieval shapes</name>
  <files>tests/test_persistence_contract.py, agents/debate_persistence.py</files>
  <action>Extend the retrieval contract tests so they prove the summary list shape stays lightweight and the detail path stays snapshot-first. Cover empty-list semantics and explicit missing-detail behavior at the helper boundary without expanding into FastAPI route tests yet.</action>
  <verify>Run the focused persistence/retrieval contract tests and confirm failures would point directly at list/detail drift</verify>
  <done>Automated coverage exists for the reusable retrieval contract before route wiring begins</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Retrieval helpers can list saved sessions by `id_card`
- [ ] Retrieval helpers can load one saved session by `session_id`
- [ ] Summary rows are newest-first and remain summary-only
- [ ] Detail loading is snapshot-first and explicit missing-session behavior exists
- [ ] Focused retrieval contract tests pass
</verification>

<success_criteria>

- All tasks completed
- The reusable retrieval layer is in place before API exposure
- Summary/list and detail/session read behavior matches Phase 2 decisions
- The retrieval contract is guarded by focused tests before route integration begins

</success_criteria>

<output>
After completion, create `.planning/phases/02-retrieval-apis/02-01-SUMMARY.md`
</output>
