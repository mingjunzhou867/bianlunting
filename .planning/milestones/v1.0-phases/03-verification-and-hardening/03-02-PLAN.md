---
phase: 03-verification-and-hardening
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [docs/saved-session-history.md, README.md, frontend/README.md, frontend/vite.config.js]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]
user_setup: []
must_haves:
  truths:
    - "Maintainers can discover the saved-session storage model, retrieval entry points, and frontend history prerequisites without reading planning artifacts"
    - "Local setup guidance clearly states that the updated MySQL DDL must be applied before retrieval APIs and the history UI are exercised"
    - "Verification documentation points to the actual focused commands produced in Phase 3 rather than vague manual instructions"
    - "Known caveats such as mock-data reset behavior and the current frontend chunk warning are documented explicitly"
  artifacts:
    - path: "docs/saved-session-history.md"
      provides: "Canonical guide for saved-session storage, retrieval, history UI dependency, and local verification"
      min_lines: 120
    - path: "frontend/README.md"
      provides: "Frontend-specific setup and history UI guidance instead of the default starter template"
      min_lines: 30
  key_links:
    - from: "docs/saved-session-history.md"
      to: "data/schema/mysql_ddl.sql"
      via: "documentation must anchor the runtime feature to the schema prerequisite"
      pattern: "debate_session|agent_debate_log"
    - from: "docs/saved-session-history.md"
      to: "api/main.py"
      via: "guide should name the retrieval and live endpoints that make up the save/read/view chain"
      pattern: "/api/debates|/api/debate_stream"
---

<objective>
Document the saved-session feature so future developers can set it up, verify it, and extend it without rediscovering hidden prerequisites.

Purpose: Turn today's working knowledge about schema setup, retrieval usage, history UI dependencies, and known caveats into durable project documentation.
Output: A canonical saved-session guide plus refreshed README surfaces and any tiny low-risk operational notes needed for the current build/setup story.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-and-hardening/03-CONTEXT.md
@.planning/phases/03-verification-and-hardening/03-RESEARCH.md
@.planning/phases/03-verification-and-hardening/03-01-PLAN.md
@README.md
@frontend/README.md
@data/schema/mysql_ddl.sql
@data/mock_data/personas_mock.sql
@api/main.py
@frontend/vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write one canonical maintainer guide for the saved-session chain</name>
  <files>docs/saved-session-history.md, data/schema/mysql_ddl.sql, data/mock_data/personas_mock.sql, api/main.py</files>
  <action>Create a dedicated document that explains the storage model, the two saved-session tables, the retrieval endpoints, the frontend history dependency on those records, and the local setup order for schema plus optional mock data.</action>
  <verify>Review the guide to confirm a new maintainer could understand how save, retrieve, and render connect without digging through planning files or source code first</verify>
  <done>The project has one discoverable source of truth for saved-session setup and behavior</done>
</task>

<task type="auto">
  <name>Task 2: Replace weak README surfaces with feature-aware entry points</name>
  <files>README.md, frontend/README.md, docs/saved-session-history.md</files>
  <action>Update the repo and frontend README files so they point developers to the saved-session guide, describe the history UI at a high level, and list the standard verification commands introduced in Phase 3 instead of leaving generic template text in place.</action>
  <verify>Check that the README updates are concise, accurate, and actually improve discoverability of the saved-session feature and verification workflow</verify>
  <done>The first documents developers open now route them toward the correct saved-session setup and validation flow</done>
</task>

<task type="auto">
  <name>Task 3: Capture current operational caveats and only apply tiny low-risk build notes if justified</name>
  <files>docs/saved-session-history.md, frontend/vite.config.js</files>
  <action>Document the DDL prerequisite, the destructive nature of the mock reset script, and the current large-chunk build warning. If execution reveals an obvious one-line or otherwise tiny low-risk build note/config improvement, include it; otherwise keep this plan documentation-first rather than forcing a performance tweak.</action>
  <verify>Confirm the final result makes known setup/build caveats explicit without expanding the phase into a separate optimization or operations project</verify>
  <done>Known risks are visible and future local setup failures are less likely to be surprising</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] There is one canonical saved-session guide under `docs/`
- [ ] README surfaces no longer hide the history feature behind template or outdated text
- [ ] Schema prerequisite, mock reset behavior, and verification commands are explicit
- [ ] Known build/setup caveats are documented clearly
- [ ] Any code-level build tweak remains truly small and low risk, or is intentionally omitted
</verification>

<success_criteria>

- All tasks completed
- Saved-session setup and verification are understandable without planning artifacts
- Future maintainers have clear instructions for schema, mock data, APIs, and frontend history dependencies
- Operational caveats are documented instead of remaining tribal knowledge

</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-hardening/03-02-SUMMARY.md`
</output>
