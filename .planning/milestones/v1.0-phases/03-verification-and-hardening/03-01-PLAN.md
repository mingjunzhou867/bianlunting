---
phase: 03-verification-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_debate.py, tests/test_persistence_contract.py, tests/test_retrieval_api.py, frontend/src/App.vue, frontend/package.json]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, READ-03, HIST-03, HIST-04]
user_setup: []
must_haves:
  truths:
    - "Automated checks protect the save -> retrieve -> render contract rather than only isolated backend helpers"
    - "Backend regression coverage still proves completed sessions persist correctly and can be read back in the snapshot-first shape the UI depends on"
    - "Frontend gets at least one lightweight automated smoke path for the new history/live state transitions without expanding into a large new test framework project"
    - "The existing live debate flow and saved-session browsing flow remain validated together with build/test commands that are practical in this repo"
  artifacts:
    - path: "tests/test_persistence_contract.py"
      provides: "Extended contract coverage for snapshot/detail/history behavior and schema/runtime drift checks"
      min_lines: 120
    - path: "frontend/package.json"
      provides: "A repeatable frontend smoke-test command, ideally without heavy new infrastructure"
      min_lines: 20
  key_links:
    - from: "tests/test_retrieval_api.py"
      to: "api/main.py"
      via: "route-level retrieval tests continue locking the public saved-session contract"
      pattern: "/api/debates|/api/debate_stream"
    - from: "frontend/src/App.vue"
      to: ".planning/phases/03-verification-and-hardening/03-CONTEXT.md"
      via: "frontend smoke extraction should protect the exact state transitions Phase 3 locked as high-value"
      pattern: "history|selectedSessionId|liveLoading"
---

<objective>
Strengthen automated confidence around the completed-session save/read/render flow with the smallest practical test expansion.

Purpose: Catch contract drift and UI-state regressions before they reach local demos or later feature work.
Output: Expanded backend regression coverage plus a lightweight automated frontend smoke check for the new history/live state machine.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-and-hardening/03-CONTEXT.md
@.planning/phases/03-verification-and-hardening/03-RESEARCH.md
@tests/test_debate.py
@tests/test_persistence_contract.py
@tests/test_retrieval_api.py
@frontend/src/App.vue
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend regression coverage around the completed-session contract</name>
  <files>tests/test_debate.py, tests/test_persistence_contract.py, tests/test_retrieval_api.py</files>
  <action>Add or refine tests so the important save/read chain is harder to break silently. Focus on the completed-session shape the history UI depends on, explicit missing/error semantics, and the DDL/runtime coupling already identified as a primary drift risk.</action>
  <verify>Review the test additions to confirm they still target the Phase 3 boundary: completed-session persistence, retrieval, and stable payload shape for downstream rendering</verify>
  <done>The backend test suite gives stronger confidence that saved-session contract drift will be caught early</done>
</task>

<task type="auto">
  <name>Task 2: Add a tiny automated frontend smoke layer for history/live state transitions</name>
  <files>frontend/src/App.vue, frontend/package.json</files>
  <action>Extract the smallest practical slice of state-shaping or transition logic from the history/live flow into testable code and wire a repeatable smoke-test command. Prefer a no-new-or-minimal-dependency path that validates the locked journeys: blank state, history selection, live result normalization, and post-run history refresh behavior.</action>
  <verify>Inspect the extracted logic and smoke command to confirm this remains lightweight, directly tied to the new history feature, and does not balloon into a full frontend testing platform</verify>
  <done>The frontend has automated confidence beyond build-only validation for the key Phase 2.1 interactions</done>
</task>

<task type="auto">
  <name>Task 3: Verify the cross-layer hardening path with practical local commands</name>
  <files>tests/test_debate.py, tests/test_persistence_contract.py, tests/test_retrieval_api.py, frontend/package.json</files>
  <action>Run the focused Python regression suite and the frontend smoke/build path that now protects the history UI. Keep the command set small and repeatable so it can be documented later as the standard local verification loop.</action>
  <verify>Confirm the final command set covers backend save/read protections plus the frontend history smoke path and production build validation</verify>
  <done>Phase 3 has a concrete, repeatable automated verification loop for the saved-session chain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend tests still verify completed-session persistence and retrieval semantics
- [ ] Schema/runtime drift remains explicitly guarded by automated checks
- [ ] Frontend has a lightweight automated smoke path for history/live state transitions
- [ ] The verification commands are practical to rerun locally
- [ ] Frontend build still passes after the hardening changes
</verification>

<success_criteria>

- All tasks completed
- The save/read/render chain is protected by stronger automated checks
- Frontend hardening remains intentionally lightweight
- Future schema or payload drift is more likely to fail in tests than in the demo UI

</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-hardening/03-01-SUMMARY.md`
</output>
