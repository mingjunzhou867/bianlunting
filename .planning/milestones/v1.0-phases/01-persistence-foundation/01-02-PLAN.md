---
phase: 01-persistence-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [agents/debate_orchestrator.py, agents/debate_persistence.py, api/main.py]
autonomous: true
requirements: [PERS-01, PERS-02]
user_setup: []
must_haves:
  truths:
    - "A successful `/api/debate` run persists one canonical successful debate session"
    - "A successful `/api/debate_stream` run persists one canonical successful debate session"
    - "Streaming behavior still reaches the frontend with the existing live debate events"
  artifacts:
    - path: "agents/debate_orchestrator.py"
      provides: "Successful-session persistence integration for both debate paths"
      min_lines: 80
    - path: "agents/debate_persistence.py"
      provides: "Persistence write helpers used by the orchestrator"
      min_lines: 40
    - path: "api/main.py"
      provides: "Existing endpoint entry points preserved while persistence integration stays internal"
      contains: "/api/debate_stream"
  key_links:
    - from: "agents/debate_orchestrator.py"
      to: "agents/debate_persistence.py"
      via: "successful session serialization and write calls"
      pattern: "debate_persistence"
    - from: "api/main.py"
      to: "agents/debate_orchestrator.py"
      via: "existing debate endpoint invocation"
      pattern: "run_debate|run_debate_stream"
---

<objective>
Integrate the aligned persistence contract into the runtime debate workflow without breaking the existing live experience.

Purpose: Make successful debate sessions actually durable when users hit `/api/debate` or `/api/debate_stream`.
Output: Orchestrator and endpoint flow updated so successful sessions are saved through the dedicated persistence helper.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-persistence-foundation/01-01-SUMMARY.md
@agents/debate_orchestrator.py
@agents/debate_persistence.py
@api/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire successful-session persistence into the non-stream debate flow</name>
  <files>agents/debate_orchestrator.py, agents/debate_persistence.py</files>
  <action>Update `run_debate()` so that once a debate successfully completes, the canonical session summary and full snapshot are persisted through the dedicated helper. Keep the phase decision intact: only successful completed sessions are stored, and failed/incomplete runs are not written as history records.</action>
  <verify>Run the existing debate flow smoke path and confirm the non-stream result still returns the expected response structure while the save hook executes on success</verify>
  <done>`run_debate()` persists one successful completed session through the aligned contract</done>
</task>

<task type="auto">
  <name>Task 2: Wire successful-session persistence into the stream debate flow</name>
  <files>agents/debate_orchestrator.py, agents/debate_persistence.py</files>
  <action>Update `run_debate_stream()` so it persists the successful completed session after the debate reaches its final result, without breaking the existing SSE event contract. Preserve current `evidence`, per-agent updates, and `debate_final` behavior for the frontend.</action>
  <verify>Review the stream path to confirm persistence happens after successful completion and SSE event emission semantics remain unchanged</verify>
  <done>`run_debate_stream()` saves successful sessions while keeping live stream compatibility</done>
</task>

<task type="auto">
  <name>Task 3: Keep API entry points stable while persistence stays an internal concern</name>
  <files>api/main.py, agents/debate_orchestrator.py</files>
  <action>Confirm the API layer still accepts the same request shape and returns the same top-level response/stream behavior. Limit any API changes to internal wiring or clearer error propagation. Do not add history endpoints here; that belongs to Phase 2.</action>
  <verify>Compare API handler signatures and current live return/stream shape before and after changes</verify>
  <done>Existing callers can keep using `/api/debate` and `/api/debate_stream` without integration changes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend debate paths still compile and run
- [ ] Successful `/api/debate` and `/api/debate_stream` completions both persist one canonical session
- [ ] Existing live event/response shape remains compatible
</verification>

<success_criteria>

- All tasks completed
- Both debate entry paths persist successful sessions through the shared contract
- No Phase 2 retrieval work leaks into this plan
- Existing frontend live-status experience remains intact

</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-02-SUMMARY.md`
</output>
