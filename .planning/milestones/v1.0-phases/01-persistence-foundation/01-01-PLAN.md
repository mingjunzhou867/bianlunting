---
phase: 01-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [data/schema/mysql_ddl.sql, agents/debate_persistence.py, .planning/phases/01-persistence-foundation/01-CONTEXT.md]
autonomous: true
requirements: [PERS-03, PERS-04, PERS-05, PERS-06]
user_setup: []
must_haves:
  truths:
    - "Completed debate sessions have one aligned persistence contract shared by MySQL DDL and runtime code"
    - "The persistence contract can represent both history list summaries and full session detail snapshots"
    - "A successful session can store person identity, execution metadata, evidence snapshot, and round-by-round judgments"
  artifacts:
    - path: "data/schema/mysql_ddl.sql"
      provides: "Aligned debate persistence schema"
      contains: "agent_debate_log"
    - path: "agents/debate_persistence.py"
      provides: "Session summary and snapshot serialization helpers"
      min_lines: 40
  key_links:
    - from: "agents/debate_persistence.py"
      to: "data/schema/mysql_ddl.sql"
      via: "matching persisted field contract"
    - from: "agents/debate_persistence.py"
      to: ".planning/phases/01-persistence-foundation/01-CONTEXT.md"
      via: "summary/detail shape matches locked context decisions"
---

<objective>
Define the canonical persistence contract for successful debate sessions before any runtime integration work begins.

Purpose: Remove the current schema/runtime drift and lock the saved data shape around the user's agreed boundary.
Output: An aligned MySQL schema definition and a dedicated backend helper that assembles session summaries and full snapshots from existing runtime objects.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-foundation/01-CONTEXT.md
@data/schema/mysql_ddl.sql
@agents/debate_orchestrator.py
@agents/base_agent.py
@evidence/evidence_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current persistence drift and codify the target storage model</name>
  <files>data/schema/mysql_ddl.sql, agents/debate_orchestrator.py, .planning/phases/01-persistence-foundation/01-CONTEXT.md</files>
  <action>Compare the fields currently emitted by the orchestrator with the `agent_debate_log` schema and the locked context decisions. Decide on the canonical stored shape for successful sessions only. Ensure the contract explicitly supports future history UX needs: history list summaries on the left and full detail snapshots in the center, without pulling frontend implementation into this phase.</action>
  <verify>Manually cross-check that every required field from PERS-03/PERS-04/PERS-05 appears in the target contract and no old mismatched column names remain ambiguous</verify>
  <done>A clear target contract exists for session summary fields, evidence snapshot fields, and per-round judgment fields</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the MySQL persistence schema around the canonical contract</name>
  <files>data/schema/mysql_ddl.sql</files>
  <action>Update the debate persistence schema so the DDL matches the runtime contract rather than the current mismatched hybrid. Preserve query-friendly summary fields for future `id_card` history lists and keep room for a full snapshot payload. Avoid frontend-specific columns; stay focused on durable backend data structures.</action>
  <verify>Review the DDL to confirm column names and semantics line up with the canonical runtime contract and summary/detail retrieval needs</verify>
  <done>`mysql_ddl.sql` expresses the same persistence model the runtime will write</done>
</task>

<task type="auto">
  <name>Task 3: Add a dedicated persistence helper for session serialization</name>
  <files>agents/debate_persistence.py</files>
  <action>Create a dedicated helper module that turns existing debate runtime objects into a session summary record and a full saved snapshot. Keep the conversion logic out of `debate_orchestrator.py` so later read APIs and tests can reuse it. The helper should assemble list-friendly metadata and detail-friendly payloads from the same source of truth.</action>
  <verify>python -m compileall agents</verify>
  <done>A reusable helper exists for building aligned session summary and snapshot payloads</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall agents` succeeds
- [ ] `data/schema/mysql_ddl.sql` matches the new persistence helper contract
- [ ] The contract clearly supports future summary-list and full-detail retrieval
</verification>

<success_criteria>

- All tasks completed
- The canonical persistence contract is defined in one place and reflected in DDL
- Runtime integration can proceed without guessing field names or storage shape
- The resulting contract supports future ChatGPT/Gemini-style history UX without expanding Phase 1 into a frontend phase

</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-01-SUMMARY.md`
</output>
