---
phase: 01-persistence-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [agents/debate_orchestrator.py, tests/test_debate.py, tests/test_persistence_contract.py]
autonomous: true
requirements: [COMP-03]
user_setup: []
must_haves:
  truths:
    - "Persistence failures are surfaced clearly instead of being silently ignored"
    - "The codebase has automated coverage for the successful-session persistence contract"
    - "Schema/runtime drift around debate persistence is detectable before Phase 2 begins"
  artifacts:
    - path: "agents/debate_orchestrator.py"
      provides: "Explicit persistence failure handling"
      min_lines: 80
    - path: "tests/test_debate.py"
      provides: "End-to-end persistence behavior checks"
      contains: "run_debate"
    - path: "tests/test_persistence_contract.py"
      provides: "Focused contract checks for debate persistence"
      min_lines: 40
  key_links:
    - from: "tests/test_debate.py"
      to: "agents/debate_orchestrator.py"
      via: "successful debate execution path"
      pattern: "DebateOrchestrator"
    - from: "tests/test_persistence_contract.py"
      to: "data/schema/mysql_ddl.sql"
      via: "contract assertions against persistence schema"
---

<objective>
Make the new persistence foundation safe to build on by surfacing errors explicitly and adding targeted contract checks.

Purpose: Prevent the project from reaching Phase 2 with a silent or drifting persistence layer.
Output: Clear persistence failure handling plus focused regression tests for the new storage contract.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-persistence-foundation/01-02-SUMMARY.md
@agents/debate_orchestrator.py
@tests/test_debate.py
@data/schema/mysql_ddl.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent persistence failures with explicit, actionable handling</name>
  <files>agents/debate_orchestrator.py</files>
  <action>Review the current persistence error handling and remove any behavior where write failures are only logged and then effectively disappear. Keep the phase rule that only successful completed sessions are saved, but make it obvious in code paths and logs when the save step fails so later debugging and verification are trustworthy.</action>
  <verify>Inspect the orchestrator flow to confirm persistence failures now change control flow or error visibility in a deliberate way</verify>
  <done>Persistence failure behavior is explicit and no longer silently swallowed</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end checks for successful session persistence</name>
  <files>tests/test_debate.py, tests/test_persistence_contract.py</files>
  <action>Create focused automated checks that prove a successful debate saves the expected canonical session contract. One test should exercise the existing end-to-end flow; another should assert the persistence contract itself so schema/runtime drift becomes visible early.</action>
  <verify>Run the relevant persistence-focused test commands and confirm failures point clearly at contract mismatches</verify>
  <done>Automated checks exist for successful-session save behavior and contract integrity</done>
</task>

<task type="auto">
  <name>Task 3: Leave Phase 2 with a stable handoff for retrieval work</name>
  <files>tests/test_persistence_contract.py, agents/debate_orchestrator.py</files>
  <action>Ensure the assertions cover the summary fields future history lists need and the detail snapshot future center-pane detail views need. Do not implement retrieval APIs here; just verify Phase 2 will inherit a stable, queryable persistence base.</action>
  <verify>Review the tests and saved contract coverage against the future history UX expectations captured in context</verify>
  <done>The persistence foundation is verified strongly enough that Phase 2 can focus on read APIs rather than rediscovering storage problems</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Persistence failures are explicit in runtime behavior
- [ ] Persistence-focused automated checks pass
- [ ] The saved contract covers both summary-list and full-detail future retrieval needs
</verification>

<success_criteria>

- All tasks completed
- Persistence failure handling is explicit
- Contract regression checks exist
- Phase 2 can start from a stable saved-session foundation

</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-03-SUMMARY.md`
</output>
