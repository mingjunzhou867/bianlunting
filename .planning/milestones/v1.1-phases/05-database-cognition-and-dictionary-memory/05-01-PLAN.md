---
phase: 05-database-cognition-and-dictionary-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/05-database-cognition-and-dictionary-memory/05-CONTEXT.md, .planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md]
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
user_setup: []
must_haves:
  truths:
    - "Dictionary artifacts stay close to the ADC310 style rather than being replaced by an overly abstract schema"
    - "Dictionary files are named by semantic dictionary ID and can serve multiple source fields"
    - "The contract distinguishes reusable dictionary knowledge from task-time semantic packaging"
  artifacts:
    - path: ".planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md"
      provides: "Formal dictionary artifact contract for the ADC310-like enhanced family"
      min_lines: 80
    - path: ".planning/phases/05-database-cognition-and-dictionary-memory/05-CONTEXT.md"
      provides: "Locked Phase 5 decisions on naming, reuse, and enhancement scope"
      min_lines: 100
  key_links:
    - from: ".planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md"
      to: "dicts/ADC310.json"
      via: "the contract should clearly evolve the existing example instead of ignoring it"
      pattern: "name|description|values|common_values"
    - from: ".planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md"
      to: ".planning/phases/05-database-cognition-and-dictionary-memory/05-CONTEXT.md"
      via: "dictionary ID naming and source_refs reuse must match the locked discussion decisions"
      pattern: "source_refs|aliases|notes|dictionary ID"
---

<objective>
Write the formal dictionary artifact contract for the new database-cognition layer.

Purpose: Define what a reusable dictionary asset looks like before later phases decide how tasks consume excerpts from it.
Output: One design document that specifies the ADC310-like enhanced structure, naming convention, metadata extensions, and multi-field reuse rules.
</objective>

<execution_context>
@C:/Users/afrangry/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/afrangry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-database-cognition-and-dictionary-memory/05-CONTEXT.md
@.planning/phases/05-database-cognition-and-dictionary-memory/05-RESEARCH.md
@dicts/ADC310.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize the enhanced ADC310-style dictionary structure</name>
  <files>.planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md, dicts/ADC310.json</files>
  <action>Write the first formal contract for dictionary artifacts by evolving the ADC310 example instead of replacing it. Keep the current readable top-level shape, then define what `source_refs`, `aliases`, and `notes` mean and why they exist.</action>
  <verify>Review the contract and confirm a reader can see exactly how the enhanced format extends the ADC310 example without losing its readability</verify>
  <done>The project has one agreed dictionary artifact shape instead of an informal example-only convention</done>
</task>

<task type="auto">
  <name>Task 2: Define naming, reuse, and source-field linkage rules</name>
  <files>.planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md, .planning/phases/05-database-cognition-and-dictionary-memory/05-CONTEXT.md</files>
  <action>Spell out that filenames follow dictionary IDs, not physical fields, and that one dictionary can serve multiple fields via `source_refs`. Clarify what later field-specific overrides may look like conceptually, without designing the override mechanism in detail yet.</action>
  <verify>Confirm the resulting contract makes it obvious why duplicate per-field dictionary files are not the default path</verify>
  <done>The dictionary family has stable naming and reuse rules for future expansion</done>
</task>

<task type="auto">
  <name>Task 3: Keep Phase 5 focused on reusable knowledge, not task-time packaging</name>
  <files>.planning/phases/05-database-cognition-and-dictionary-memory/05-DICT-CONTRACT.md</files>
  <action>Make the document explicit about what belongs in long-lived dictionary assets versus what belongs later in task-trimmed semantic packets. Prevent the contract from absorbing prompt-time excerpting logic that should live in Plan 05-02.</action>
  <verify>Read the document top to bottom and confirm it defines reusable assets only, not the downstream packet contract</verify>
  <done>The dictionary contract is cleanly separated from semantic-packet design</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The enhanced dictionary contract still clearly resembles ADC310-style artifacts
- [ ] Naming is based on dictionary ID rather than physical field name
- [ ] Multi-field reuse through `source_refs` is explicit
- [ ] The document defines reusable dictionary assets, not task-time packet behavior
</verification>

<success_criteria>

- All tasks completed
- Phase 5 has a formal dictionary artifact contract grounded in the existing example
- Future dictionary generation work has a clear target format
- Later phases can consume dictionary assets without re-arguing naming and reuse rules

</success_criteria>

<output>
After completion, create `.planning/phases/05-database-cognition-and-dictionary-memory/05-01-SUMMARY.md`
</output>
